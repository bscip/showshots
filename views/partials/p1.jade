div.row-fluid
  div.span6
    h4.mini-header Search for a new Artist
    br
    div.btn-group.row-fluid
      span.span6
        input(ng-model="artist_input", type="text", class="form-control form-group")
      span.span6
        button(ng-click="artist_search()", type="submit", class="btn btn-info") Search
    div(ng-repeat='artist in artists', class="row-fluid")
      span.span3
            button(ng-click="artist_select({{artist.songkick_id}})", type="submit", class="btn-small btn-success") Find Shows
          span.span9 {{artist.name}}
  div.span6
    h4.mini-header Try Some Popular Artists
    br
    div(ng-repeat='top_artist in top_artists', class="row-fluid top-artists")
      span.span7.offset1 {{top_artist.name}}
      span.span4
        a(href='shows/{{top_artist.songkick_id}}') {{top_artist.image_count}} Images Found
  div.span12#hottt-section
    h4.mini-header Or try some of the top HOTTT artists from Echo Nest
  div.span12
    ul.inline
      li(ng-repeat='hottt in hottt_artists', class="hottt")
        a(ng-click='hottt_select({{hottt}})') {{hottt.name}}
